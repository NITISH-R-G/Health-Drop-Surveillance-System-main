--- lib/sync.ts
+++ lib/sync.ts
@@ -69,11 +69,12 @@
       // Try local storage first
       const localData = await getLocalData(key);
       if (localData) {
         setData(localData);
       } else {
         // If no local data, we fallback to initial (which could be the mock data itself)
         // and trigger a sync to populate local storage.
         setData(initialData);
       }
       setLoading(false);
     };

     loadData();
-  }, [key, initialData]);
+  }, [key]); // removed initialData to prevent infinite loop if passed inline

   return { data, loading, setData };
 }
