name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install Dependencies
      run: npm ci --legacy-peer-deps

    - name: Type Check
      run: npx tsc --noEmit

    - name: Lint
      run: npx eslint . --ext .js,.jsx,.ts,.tsx
      continue-on-error: true # Let's not fail the pipeline initially due to existing lint errors

    - name: Test
      run: npx jest
